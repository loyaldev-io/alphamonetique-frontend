[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "22"

# Redirige toutes les URLs vers index.html (SPA routing)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
